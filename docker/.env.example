# =============================================================================
# Crawlrs Docker 环境变量配置（示例）
# =============================================================================
# 复制此文件为 .env 并根据需要修改配置
# =============================================================================

# =============================================================================
# 应用基础配置
# =============================================================================
ENV_MODE=docker
RUST_LOG=info
SERVICE_TYPE=api

# =============================================================================
# 应用端口
# =============================================================================
APP_PORT=8899

# =============================================================================
# 数据库配置
# =============================================================================
DB_HOST=db
DB_PORT=5432
DB_USER=crawlrs
# 仅隔离测试环境，生产走 CRAWLRS__DATABASE__PASSWORD（confers 配置前缀）
DB_PASSWORD=strong_random_password_placeholder_16_chars_min
DB_NAME=crawlrs
DATABASE_MAX_CONNECTIONS=20

# =============================================================================
# 搜索引擎启用配置
# =============================================================================
SEARCH_ENGINE_GOOGLE_ENABLED=false
SEARCH_ENGINE_BING_ENABLED=true
SEARCH_ENGINE_BAIDU_ENABLED=true
SEARCH_ENGINE_SOGOU_ENABLED=true
SEARCH_ENGINE_DEFAULT=baidu

# =============================================================================
# Google搜索配置 (仅当 SEARCH_ENGINE_GOOGLE_ENABLED=true 时需要)
# =============================================================================
# 方式一: 使用 Google Custom Search API (推荐)
# GOOGLE_SEARCH_API_KEY=your_google_api_key
# GOOGLE_SEARCH_CX=your_search_engine_id

# 方式二: 使用 FlareSolverr 代理 (需要启动 flaresolverr 和 chrome 服务)
# FLARESOLVERR_AUTO_START=true

# =============================================================================
# FlareSolverr配置 (用于 Google 搜索代理)
# =============================================================================
FLARESOLVERR_HOST=flaresolverr
FLARESOLVERR_PORT=8191
FLARESOLVERR_AUTO_START=true
FLARESOLVERR_LOG_LEVEL=info
FLARESOLVERR_CAPTCHA_SOLVER=none

# =============================================================================
# Chrome配置
# =============================================================================
CHROME_HOST=chrome
CHROME_PORT=9222

# =============================================================================
# 速率限制配置
# =============================================================================
RATE_LIMITING_ENABLED=false
RATE_LIMITING_DEFAULT_RPM=60

# =============================================================================
# 并发配置
# =============================================================================
CONCURRENCY_DEFAULT_TEAM_LIMIT=10
CONCURRENCY_TASK_LOCK_DURATION=300

# =============================================================================
# 监控配置
# =============================================================================
METRICS_ENABLED=true
PROMETHEUS_PORT=9090
GRAFANA_PORT=3001
GRAFANA_PASSWORD=admin
PGADMIN_PORT=5050
PGADMIN_PASSWORD=admin

# =============================================================================
# 代理配置 (按需取消注释)
# =============================================================================
# HTTP_PROXY=http://proxy:port
# HTTPS_PROXY=http://proxy:port
# NO_PROXY=localhost,127.0.0.1,.local,.corp

# =============================================================================
# LLM配置 (可选)
# =============================================================================
# LLM_API_KEY=your_openai_api_key
# LLM_MODEL=gpt-3.5-turbo
# LLM_API_BASE_URL=https://api.openai.com/v1

# =============================================================================
# 快速启动命令
# =============================================================================
#   全部启动:     docker compose --profile all up -d
#   仅基础设施+应用: docker compose --profile app --profile infrastructure up -d
#   启动监控:     docker compose --profile monitoring up -d
#   启动浏览器:   docker compose --profile browser up -d
# =============================================================================
