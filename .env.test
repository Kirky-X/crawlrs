# Database configuration (using existing nebula containers)
DB_HOST=localhost
DB_PORT=5432
DB_USER=idgen
DB_PASSWORD=idgen123
DB_NAME=crawlrs_test

# Redis configuration (using existing nebula-redis)
REDIS_HOST=localhost
REDIS_PORT=6379

# Application configuration
APP_PORT=8899
RUST_LOG=info
ENV_MODE=docker

# Service configuration
CRAWLRS__DATABASE__URL=postgres://idgen:idgen123@localhost:5432/crawlrs_test
CRAWLRS__REDIS__URL=redis://localhost:6379
CRAWLRS__SERVER__HOST=0.0.0.0
CRAWLRS__SERVER__PORT=8899
CRAWLRS__SERVER__ENABLE_PORT_DETECTION=false
